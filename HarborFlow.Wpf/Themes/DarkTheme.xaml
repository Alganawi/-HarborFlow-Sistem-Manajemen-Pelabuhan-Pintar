<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="WindowBackgroundColor" Color="#FF1E1E1E"/>

    <!-- Dark Theme Colors - Enhanced Contrast -->
    <SolidColorBrush x:Key="PrimaryBackgroundColor" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="PrimaryForegroundColor" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="SecondaryForegroundColor" Color="#FFB0B0B0"/>
    <SolidColorBrush x:Key="ButtonBackgroundColor" Color="#FF0078D4"/>
    <SolidColorBrush x:Key="ButtonForegroundColor" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ButtonHoverBackgroundColor" Color="#FF1E8FE6"/>
    <SolidColorBrush x:Key="TextBoxBackgroundColor" Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="TextBoxForegroundColor" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="TextBoxBorderColor" Color="#FF3F3F46"/>
    <SolidColorBrush x:Key="ListViewBackgroundColor" Color="#FF252526"/>
    <SolidColorBrush x:Key="CardBackgroundColor" Color="#FF2D2D30"/>

    <!-- Button Style -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundColor}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundColor}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonHoverBackgroundColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox Style -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource TextBoxBackgroundColor}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBoxForegroundColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- ListView Style -->
    <Style TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource ListViewBackgroundColor}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <!-- ComboBox Style -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource TextBoxBackgroundColor}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBoxForegroundColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorderColor}"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <!-- TextBlock Style -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundColor}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Label Style -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundColor}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <Style x:Key="HyperlinkButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#FF4FC3F7"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent" 
                            BorderThickness="0"
                            Padding="2">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="0"
                                Padding="2">
                            <TextBlock Text="{TemplateBinding Content}" 
                                       TextDecorations="Underline" 
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#FF81D4FA"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" Value="#FFB3E5FC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Icon Paths -->
    <PathGeometry x:Key="SunIcon">M12,9.5A2.5,2.5 0 0,0 9.5,12A2.5,2.5 0 0,0 12,14.5A2.5,2.5 0 0,0 14.5,12A2.5,2.5 0 0,0 12,9.5M12,2L14.39,5.42L18.17,6.17L15.09,9.19L15.83,13.05L12,11.25L8.17,13.05L8.91,9.19L5.83,6.17L9.61,5.42L12,2Z</PathGeometry>
    <PathGeometry x:Key="MoonIcon">M12,2A9.91,9.91 0 0,0 9.5,2.16A10,10 0 0,1 9.5,21.84A10,10 0 0,0 12,22A10,10 0 0,0 12,2Z</PathGeometry>

    <!-- ListViewItem Style -->
    <Style TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                        <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHoverBackgroundColor}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonBackgroundColor}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GridViewColumnHeader Style -->
    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundColor}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorderColor}"/>
    </Style>

    <!-- Sidebar Resources -->
    <LinearGradientBrush x:Key="SidebarBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF052767" Offset="0.0"/>
        <GradientStop Color="#FF3A0647" Offset="0.7"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="SidebarForegroundBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="SidebarHoverBrush" Color="#30FFFFFF"/>
    <SolidColorBrush x:Key="SidebarSelectedBrush" Color="#50FFFFFF"/>

    <!-- Navigation ListBox Style -->
    <Style x:Key="NavListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource SidebarForegroundBrush}"/>
    </Style>

    <Style x:Key="NavListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="15,12"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Foreground" Value="{StaticResource SidebarForegroundBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="border" Background="Transparent" CornerRadius="4">
                        <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarHoverBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarSelectedBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PasswordBox Style -->
    <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="{StaticResource TextBoxBackgroundColor}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBoxForegroundColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

</ResourceDictionary>
